# setup instruction of state

```text
S [number of Eventhandler]
    E [libray number] [clone number + G-L bool + number of process]
        P [pPos] [cPos]
        P [pPos] [cPos]
    E [libray number] [clone number + G-L bool + number of process]
        P [pPos] [cPos]
        P [pPos] [cPos]
```