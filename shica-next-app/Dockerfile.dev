# 開発用Dockerfile
FROM node:18-alpine

# 作業ディレクトリ作成
WORKDIR /app

# 必要なシステムパッケージをインストール（WebSocket関連）
RUN apk add --no-cache libc6-compat

# package.jsonとpackage-lock.jsonをコピー
COPY package*.json ./

# 依存関係インストール（開発用も含む）
RUN npm install

# 残りのファイルをコピー
COPY . .

# ポート3000を公開
EXPOSE 3000

# 開発サーバー起動
CMD ["npm", "run", "dev"]