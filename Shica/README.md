# Shica 


## RULE 

Function return 0 should be true and Function return 1 should be false.
Because it is used at after condition like,

```C
int webrun(){
    int ret = func();
    if(ret)return 1;
}
```

